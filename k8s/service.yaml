apiVersion: v1
kind: Service
metadata:
  name: roomspeak-svc
  namespace: room-speak-prod
  labels:
    app: roomspeak
    environment: production
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: roomspeak