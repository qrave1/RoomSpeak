apiVersion: v1
kind: Service
metadata:
  name: roomspeak-svc
  namespace: roomspeak-prod
  labels:
    app: roomspeak
    environment: production
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: roomspeak